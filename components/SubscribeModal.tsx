"use client";

import Modal from "./Modal";

const SubsribeModal = () => {
  return (
    <Modal
      title="Only for premium users"
      description="Listen to music with SoundVoyage Premium"
      isOpen
      onChange={() => {}}
    >
      Subscription
    </Modal>
  );
};

export default SubsribeModal;
